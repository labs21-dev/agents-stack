# 額外鏡頭育成日誌（Emerging Lenses Log）

這個檔案記錄六標準鏡頭之外被引入過的額外鏡頭。目的：讓「該加的鏡頭沒加」與「反覆出現的鏡頭該畢業成標準鏡頭」這兩件事可追蹤，使框架隨使用演化。

> 這是模板。真實的鏡頭記錄由使用者在自己的 runtime 累積，不隨模板散布。

## 記錄格式

每筆一個區塊：

```
## [日期] 額外鏡頭名稱 — 用於什麼問題
- 問題類型（見 routing-table.md）：
- 觸發變數：步驟 2 抽出的哪個核心變數需要它
- 反事實對比：硬套六鏡哪一個會漏掉什麼
- 觀察推論摘要：（該鏡頭產出的關鍵洞察）
- 事後驗證：這個鏡頭的洞察事後被證實有效 / 無效 / 待驗
- 累計使用次數：（同類問題）
- 是否達畢業門檻：（是 → 提議納入 routing-table；否 → 仍為臨時鏡頭）
```

## 畢業門檻（建議）

- 同類問題累計 ≥ 3 次，且事後驗證至少 1 次有效 → 提議納入 `routing-table.md`。
- 累計 ≥ 3 次但全部無效 → 標記為「偽需求鏡頭」，不再輕易引入（記錄其失敗原因）。

## 記錄

## [2026-08-14] 時序/地平線 — 定義正確、長時、可信賴、可塑、可擴展、安全的 Agent 框架
- 問題類型（見 routing-table.md）：系統/agent 架構設計
- 觸發變數：權威狀態的歸屬（誰擁有跨步、跨窗、跨任務的真實紀錄）
- 反事實對比：硬套底層邏輯會把長時畫成「更多步驟的狀態機」，漏掉錯誤正相關造成的超線性可靠度衰減、H1⊂H2⊂H3 嵌套地平線、單步合法但序列目標不合法、以及 naive 情景記憶傷害長時 GDS
- 觀察推論摘要：長時不是把同一條 session 拉長，而是把任務狀態移出生成上下文；只有經環境獨立驗證的事實可以跨輪存活。Agent = π_θ ⊕ H。
- 事後驗證：待驗
- 累計使用次數：2（同類問題；同日第二題「True OS 到臨長什麼樣」複用）
- 是否達畢業門檻：否 → 仍為臨時鏡頭

## [2026-08-14] 資安/信任邊界 — 定義正確、長時、可信賴、可塑、可擴展、安全的 Agent 框架
- 問題類型（見 routing-table.md）：系統/agent 架構設計
- 觸發變數：控制面與生成面的分離（誰能改環境、誰能標記完成、誰能吸收經驗）
- 反事實對比：硬套逆向思考會把安全當成失敗模式清單，漏掉生成面預設不可信、評估意識/sandbagging、可塑性本身是攻擊面、以及安全是尾部硬約束不能平均進能力分
- 觀察推論摘要：每個副作用動作必須先過控制面；每個完成聲明必須先過獨立審計；經驗吸收不得改不變量。安全與能力分開計量。
- 事後驗證：待驗
- 累計使用次數：2（同類問題；同日第二題「True OS 到臨長什麼樣」複用）
- 是否達畢業門檻：否 → 仍為臨時鏡頭

## [2026-08-14] 時序/地平線 — True OS for Agentic World 的到臨長什麼樣
- 問題類型（見 routing-table.md）：系統/agent 架構設計（橫跨產品/商業策略）
- 觸發變數：到臨的嵌套地平線（什麼時候算到了、沿途會長什麼樣）
- 反事實對比：硬套底層邏輯會把 True OS 畫成一次到位的終態架構圖，漏掉到臨是閾值穿越而非功能齊備、H1（有 ground truth 的 kernel）⊂ H2（組織默認底物）⊂ H3（agent 互開機）、以及第一個可見形狀不是最終形狀
- 觀察推論摘要：到臨的可見形狀是工作單元切換，不是品牌發表。H1 看起來像「停止寫 ReAct loop、completed 不再是模型 token」；H2 看起來像例外隊列 + 權能票據；H3 看起來像別的 agent 開進同一內核。假到臨會長得像更好的 chatbot。
- 事後驗證：待驗
- 累計使用次數：2（同類問題）
- 是否達畢業門檻：否 → 仍為臨時鏡頭

## [2026-08-14] 資安/信任邊界 — True OS for Agentic World 的到臨長什麼樣
- 問題類型（見 routing-table.md）：系統/agent 架構設計（橫跨產品/商業策略）
- 觸發變數：內核權威的不可繞過性（誰寫 Σ、誰寫 completed、誰發權能票據）
- 反事實對比：硬套逆向思考會把「被繞過」列成一條失敗模式，漏掉不可繞過性是 OS 的定義條件而非風險項、生成面預設不可信、以及 chat 表面一旦擁有 completed/權限就不再是 tty 而是偽內核
- 觀察推論摘要：True OS 到臨的判準不是「有記憶、有工具、有排程」，而是副作用無票不跑、completed 只有 Checker 能寫、模型是 CPU 不是內核。誰讓生成面寫這三件事，誰就還不是 OS。
- 事後驗證：待驗
- 累計使用次數：2（同類問題）
- 是否達畢業門檻：否 → 仍為臨時鏡頭

## [2026-08-14] 生態/外部性 — True OS for Agentic World 的到臨長什麼樣
- 問題類型（見 routing-table.md）：系統/agent 架構設計（橫跨產品/商業策略）
- 觸發變數：互補品鎖定（別人是否把 skill/agent/工具寫進你的 syscall，而不是繞過你）
- 反事實對比：硬套經濟學會講誘因與轉換成本，漏掉 OS 是「他人寫入你 ABI 之後形成的環境」這一外部性、雞生蛋（沒 app 不成 OS、沒 OS 沒人寫 app）、以及模型實驗室用相容性捕食把獨立內核降級成 libc
- 觀察推論摘要：到臨不是你的 kernel 做完，而是繞過你的人開始變窮——他們的 agent 無法跟別人的工具/狀態/完成位對齊。POSIX 時刻先於 Windows 時刻；MCP 是 USB 不是 OS。
- 事後驗證：待驗
- 累計使用次數：1（同類問題）
- 是否達畢業門檻：否 → 仍為臨時鏡頭

## [2026-08-26] 時序/地平線 — 如何用 AI 寫一個連載故事
- 問題類型（見 routing-table.md）：創作/內容決策（橫跨系統設計：AI 協作流程）
- 觸發變數：連載的狀態耐久性（故事世界狀態必須跨 N 集、跨 context window 存活且保持一致）
- 反事實對比：硬套六鏡會把連載畫成「寫長一點的故事」，漏掉漂移是跨集累積而非單集可見、LLM 無跨 session 記憶導致狀態必須外移、以及「現在斷更讀者流失 vs 之後後宮化流失」的時間結構
- 觀察推論摘要：連載的失敗率不是每集品質的函數，而是「狀態外移完整度 × 集數」的衰減曲線。只有經 canon gate 驗證過的事實可以跨集存活。與既有記憶：跟 ParallelPaws 憲法 gate 同一數學結構。
- 事後驗證：待驗
- 累計使用次數：3（同類問題：Agent 框架、True OS 到臨、本次）
- 是否達畢業門檻：**是**（≥3 次同類問題）→ 建議提議納入 routing-table.md 作為候選標準鏡頭（事後驗證 ≥1 次有效後正式納入）

## [2026-09-09] 資安/信任邊界 — 信箋 prompt 多輪仍不合格
- 問題類型（見 routing-table.md）：系統/agent 架構設計（橫跨產品聲音）
- 觸發變數：控制面與生成面的分離（誰有權決定「這封信已落地」）
- 反事實對比：硬套心理學會講使用者信任／陪伴感，硬套底層邏輯會繼續在 lettercore 裡加原則；兩者都漏掉 GLM 輸出預設不可信、憲法不是 kernel、HostJob 已有 voice lint 而 agent 信箋沒有獨立 verifier
- 觀察推論摘要：三輪只轉 lettercore 這顆旋鈕，是讓生成面寫 completed。值班句是 decoder 先驗，不是缺一條禁令。新方向是把「落地」移出模型：剪裁或獨立檢查，憲法保持薄。
- 事後驗證：待驗
- 累計使用次數：3（Agent 框架、True OS、本次）
- 是否達畢業門檻：次數已滿，事後驗證仍待驗 → 不正式納入 routing-table

## [2026-09-09] 時序/地平線 — 信箋 prompt 多輪仍不合格
- 問題類型（見 routing-table.md）：系統/agent 架構設計（橫跨產品聲音）
- 觸發變數：金標分佈的地平線（單輪 40 場短場景 vs 多輪深信）
- 反事實對比：硬套頂層「換評價標準」會說 eval 設錯，漏掉原 bug 是 H2（同一張紙的 ICL 把 coda 練熟），H1 單輪 eval 看不見；優化 H1 會讓 08 變成請假函、對螢幕那封深信無效
- 觀察推論摘要：螢幕對話是序列目標不合法（每封單獨都像在陪伴，串起來是看護）。新方向必須含多輪深信金標，否則繼續局部優化。
- 事後驗證：待驗
- 累計使用次數：4（Agent 框架、True OS、連載、本次）
- 是否達畢業門檻：次數已滿，事後驗證仍待驗

## [2026-09-10] 時序/地平線 — 未來 Agent 平台的構思／功能
- 問題類型（見 routing-table.md）：系統/agent 架構設計（橫跨產品/商業策略）
- 觸發變數：工作單元的跨窗存活（Job 而非 chat turn 才是平台的時間原子）
- 反事實對比：硬套底層邏輯會把平台畫成「更長的 session + 排程 + 記憶」，漏掉 2026 市場上 Foundry/Copilot/Managed Agents 已齊備這些功能但仍是假到臨、錯誤正相關讓功能越多越快垮、以及 H1/H2/H3 的可見功能集不可混進同一張產品清單
- 觀察推論摘要：平台功能必須按嵌套地平線分層，不能做成競品功能並集。H1 功能是「completed 不再是 token」；H2 功能是例外隊列與權能；H3 功能是外來 agent 開進同一 Σ 語義。把三層攤平就是 vaporware。
- 事後驗證：待驗
- 累計使用次數：5（Agent 框架、True OS、連載、信箋、本次）
- 是否達畢業門檻：次數已滿，事後驗證仍待驗 → 不正式納入 routing-table

## [2026-09-10] 資安/信任邊界 — 未來 Agent 平台的構思／功能
- 問題類型（見 routing-table.md）：系統/agent 架構設計（橫跨產品/商業策略）
- 觸發變數：控制面與生成面的分離（誰寫 Σ、誰寫 completed、誰發票據）
- 反事實對比：硬套逆向思考會把安全列成平台的一項功能（治理、審計、HITL），漏掉這三件事若可由生成面寫入則「平台」定義不成立、Entra Agent ID / Agent 365 是身份戲服、skill marketplace 在生成面特權內執行是惡意軟體分發
- 觀察推論摘要：未來平台的產品句不是「更會做事的 agent」，而是「這個環境裡 agent 做不到某些事」。功能清單裡沒有獨立 Checker 與無票不跑，其餘功能都是 Copilot Studio 的再發行。
- 事後驗證：待驗
- 累計使用次數：4（Agent 框架、True OS、信箋、本次）
- 是否達畢業門檻：次數已滿，事後驗證仍待驗 → 不正式納入 routing-table

## [2026-09-10] 生態/外部性 — 未來 Agent 平台的構思／功能
- 問題類型（見 routing-table.md）：系統/agent 架構設計（橫跨產品/商業策略）
- 觸發變數：互補品鎖定（Pack/Skill/外來 agent 是寫進你的 syscall，還是把你當 libc 繞過）
- 反事實對比：硬套經濟學會講訂閱與轉換成本，漏掉 2026 實驗室用 Agents SDK / Managed Agents / MCP 相容性把獨立 runtime 降級成客戶端、功能市集若 ABI 不穩就沒人寫、MCP 是 USB 不是 OS
- 觀察推論摘要：平台功能的判準是「別人開始為你的完成位／票據／Σ 語義寫東西」。先賣聊天殼或白標後端，會把互補品作者趕到模型實驗室。POSIX（凍結 ABI）先於 Windows（漂亮殼）。
- 事後驗證：待驗
- 累計使用次數：2（True OS 到臨、本次）
- 是否達畢業門檻：否 → 仍為臨時鏡頭

## [2026-09-18] 時序/地平線 — shared vs isolated 記憶路徑
- 問題類型（見 routing-table.md）：系統/agent 架構設計
- 觸發變數：記憶條目的合法壽命（task / project / agent-instance）必須跟目錄契約一致
- 反事實對比：硬套底層邏輯會畫成「shared 一個檔、isolated 一棵樹」兩套 store，漏掉 working 的壽命是 task、semantic 是 project、`{agent_name}` 是 instance；isolated 若不標職責，過期 desk 會假扮成長期事實
- 觀察推論摘要：租戶軸不能取消職責軸的 TTL。shared/working.md 與 isolated/{agent}/*.md 若無 duty，就沒有「任務結束必須刪」的規則可執行。
- 事後驗證：待驗
- 累計使用次數：6（Agent 框架、True OS、連載、信箋、平台、本次）
- 是否達畢業門檻：次數已滿，事後驗證仍待驗 → 不正式納入 routing-table

## [2026-09-18] 資安/信任邊界 — shared vs isolated 記憶路徑
- 問題類型（見 routing-table.md）：系統/agent 架構設計
- 觸發變數：目錄名是否被誤當成讀寫權限
- 反事實對比：硬套逆向思考會把「agent 互讀」列成一條失敗模式，漏掉 markdown 目錄沒有 harness 就不是 ACL、生成面預設能讀整個 `.agents/`、把秘密放進 isolated/ 是假隔離
- 觀察推論摘要：isolated 最多是檢索提示（先看自己的 INDEX）。它不是安全邊界。寫閘仍是「不存秘密」；真隔離要靠 runtime 權限，不是資料夾名字。
- 事後驗證：待驗
- 累計使用次數：5（Agent 框架、True OS、信箋、平台、本次）
- 是否達畢業門檻：次數已滿，事後驗證仍待驗 → 不正式納入 routing-table
