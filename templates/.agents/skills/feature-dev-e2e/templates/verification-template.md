# Verification 規格模板

> 放在 `.agent/skills/verification.md` 或按 feature 拆檔。
> 這份文件的讀者是 agent 本身：它讀完要能**執行**驗證，不是理解概念。
> 每條驗證 = 前置 → 動作 → 斷言。斷言查結果，不查無報錯。

## <Feature 名> 驗證規格

### 環境準備
- 啟動命令：<精確命令，含環境變數>
- 測試數據：<怎麼造、造多少、造到哪>
- 清理方式：<跑完怎麼恢復原狀>

### 驗證項

#### V1 <正路徑名>
- 前置：<數據/狀態>
- 動作：<精確操作，可機器執行>
- 斷言：<可觀察的具體結果，含期望值>

#### V2 <邊界名>
- 前置：
- 動作：
- 斷言：

#### V3 <故障/恢復名>（可選但推薦）
- 前置：
- 動作：
- 斷言：

### 執行協議
- 順序執行 V1→V2→V3；任何一步失敗 → 修復 → **從 V1 重跑全部**（不是只重跑失敗項——修復可能破壞已通過的項）
- 全部通過後輸出：每項一行「V1 ✅ <斷言實際值>」格式的結果表
- 禁止：跳過任何一項、用「應該沒問題」替代執行、自我宣稱通過

### 已知陷阱（更新於每次踩坑後）
- <例：中文關鍵詞需 trim 前後空格，否則 V2 假失敗>
- <例：dev 模式首次啟動會建索引，V1 需等日志出現 ready>

### 護欄反測（驗證本身的可信度）
- 每條斷言用一個**已知壞例子**反向測過：故意引入對應 bug，斷言必須變紅
- 上次反向測試日期：<日期>